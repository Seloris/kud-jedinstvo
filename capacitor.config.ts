import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kud.jedinstvo',
  appName: 'Kud Jedinstvo',
  webDir: 'dist/kud-jedinstvo/browser',
};

export default config;
